import withApollo from "@/hoc/withApollo";
import styled from "styled-components";

const EditReview = () => {
  return <EditReviewWrapper>Edit Review Wrapper</EditReviewWrapper>;
};
export default withApollo(EditReview);

const EditReviewWrapper = styled.div``;
